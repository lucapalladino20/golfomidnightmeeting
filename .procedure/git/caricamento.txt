git remote remove origin

# Inizializzare il repository locale
git init
git add .

git commit -m "Initial commit"

# Collegare il repository locale al repository remoto
git remote add origin https://github.com/lucapalladino20/golfomidnightmeeting.git

# Caricare il progetto su GitHub
git push -u origin master
git push --force origin master
